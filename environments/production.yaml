# Service Account Configuration
serviceAccount:
  enabled: true
  name: retail-kanopy-sa-prod # Must match KSA_NAME
  annotations: {} # Placeholder for injecting GCP service account annotation via Helm/pipeline.

# Mount GCP credential config AND projected service account token
extraVolumes:
  - name: gcp-creds
    configMap:
      name: gcp-wif-config-retail-prod
  - name: gcp-token
    projected:
      sources:
        - serviceAccountToken:
            path: token
            expirationSeconds: 3600
            audience: "//iam.googleapis.com/projects/531154772515/locations/global/workloadIdentityPools/kanopy-retail-wif-pool-prod/providers/kanopy-retail-provider-prod"

extraVolumeMounts:
  - name: gcp-creds
    mountPath: /etc/gcp
    readOnly: true
  - name: gcp-token
    mountPath: /var/run/service-account
    readOnly: true

# Secret references for sensitive data (production secrets)
envSecrets:
  MONGODB_URI: retail-store-prod
  DB_NAME: retail-store-prod
  COLLECTION_NAME: retail-store-prod
  GCP_STORAGE_BUCKET: retail-store-prod
  GCP_BUCKET_FOLDER: retail-store-prod
  VERTEX_AI_ENDPOINT: retail-store-prod
  DYNAMIC_PRICING_AUDIENCE: retail-store-prod
  DYNAMIC_PRICING_START_URL: retail-store-prod
  DYNAMIC_PRICING_STOP_URL: retail-store-prod
  PORT: retail-store-prod

# Environment variables for GCP (production)
env:
  GOOGLE_APPLICATION_CREDENTIALS: /etc/gcp/credential-configuration.json
  GCP_PROJECT_ID: ist-retail-demo
  GCP_LOCATION: us-central1
  # Stop GCP client libs (google-auth-library/gcp-metadata) from probing the
  # link-local metadata endpoint 169.254.169.254 — same IP as AWS IMDS, which
  # Kanopy's IMDS audit flags. All GCP auth uses the mounted WIF file creds, so
  # the metadata server is never legitimately needed.
  GOOGLE_CLOUD_PROJECT: ist-retail-demo
  METADATA_SERVER_DETECTION: none

# Kanopy security and ownership (requires web-app chart v4.30.0+)
security:
  dataClassification: "Internal"
  dataDesignation: ["MI"]

ownership:
  driEmail: angie.guemes@mongodb.com